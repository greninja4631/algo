Jenkinsサーバ起動（Docker）

docker start jenkins


URLのポート:8080 である。
【クロスアーキテクチャ Docker Build ワンライナー】



docker buildx build -f Docker/Dockerfile --platform linux/arm64 -t yourapp:arm64 . --load && \
docker buildx build -f Docker/Dockerfile --platform linux/amd64 -t yourapp:amd64 . --load